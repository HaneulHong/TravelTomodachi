node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.env.*.local

# Capacitor
# ios/·android/는 커밋한다. 딥링크 스킴 같은 설정이 그 안에 있다.
# 빌드 결과물과 복사된 웹 파일은 각 폴더의 .gitignore가 거른다.
.capacitor

# 에디터
.vscode/*
!.vscode/extensions.json
.idea

# 전송용 임시 파일
.tt-src.tar.gz
.tt-maps-patch.tar.gz
.tt-marker-fix.tar.gz

# 타입체크 캐시
tsconfig.tsbuildinfo
.tt-mapid-warn.tar.gz

# 운영 DB 백업 (npm run backup) — 로그인 이메일이 들어 있다
backups/
